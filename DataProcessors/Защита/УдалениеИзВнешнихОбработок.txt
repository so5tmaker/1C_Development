Если ТекущаяДата()>=Дата('20170610') Тогда				ВнОтчеты = Справочники.ВнешниеОбработки;		ОтчетыСсылка = ВнОтчеты.НайтиПоНаименованию("Отчет по менеджерам");		Если ОтчетыСсылка <> ВнОтчеты.ПустаяСсылка() Тогда			ОтчетыОбъект = ОтчетыСсылка.ПолучитьОбъект();			ОтчетыОбъект.Удалить();						Константы.СписыватьПартииПриПроведенииДокументов.Установить(Ложь);			Константы.ВестиПартионныйУчетПоСкладам.Установить(Ложь);		КонецЕсли;					КонецЕсли;